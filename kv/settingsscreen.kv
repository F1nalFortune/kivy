<SettingsScreen>:
    Button:
        text:
            "You are at the settings screen"
        on_release:  app.change_screen("home_screen")
    Button:
        text:
            "Upload Blacklist"
        on_release:  app.change_screen("database_screen")
    Button:
        text:
            "Download Blacklist"
        on_release:  app.change_screen("home_screen")
